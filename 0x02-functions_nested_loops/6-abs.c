#include "main.h"

/**
 * _abs - print the absolute valur of the integer
 * @n: integer to check
 * Return: value
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	} else if (n < 0)
	{
		return (-n);
	}

		return (0);
}
